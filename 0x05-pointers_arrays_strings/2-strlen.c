#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: string whose length is to be returned
 * Return: strlen.
*/

int _strlen(char *s)
{
return (strlen(s));
}
